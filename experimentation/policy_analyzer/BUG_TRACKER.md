# Bugs to fix in the analyzer frontend
- [ ] Rollout evals not running in parallel, but are sequential
- [ ] After a rollout is done it is not immediately updated on the website. A reload of the page shows it correctly